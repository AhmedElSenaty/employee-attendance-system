/**
 * Enum representing the type of a Mission.
 * 
 * @enum {number}
 * @property {number} HalfDay - The mission is half day.
 * @property {number} FullDay - The mission is full day.
*/
export enum MissionRequestType {
    HalfDay = 0,
    FullDay = 1,
  }