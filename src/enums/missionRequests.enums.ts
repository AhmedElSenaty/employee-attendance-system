/**
 * Enum representing the status of a Mission.
 * 
 * @enum {number}
 * @property {number} HalfDay - The mission is half day.
 * @property {number} FullDay - The mission is full day.
*/
export enum MissionStatusType {
    HalfDay = 0,
    FullDay = 1,
  }